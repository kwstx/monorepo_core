[project]
name = "agent_infra_event_bus"
version = "1.0.0"
description = "Shared Event Bus for Agent Infrastructure Monorepo"
requires-python = ">=3.10"
dependencies = [
    "redis>=5.0.0",
    "pydantic>=2.0.0"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
